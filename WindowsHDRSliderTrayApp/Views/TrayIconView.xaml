<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="WindowsHDRSliderTrayApp.Views.TrayIconView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:tb="using:H.NotifyIcon"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:converters="using:WindowsHDRSliderTrayApp.Converters"
    mc:Ignorable="d">
    
    <UserControl.Resources>
        <converters:BoolToImageSourceConverter
            x:Key="BoolToImageSourceConverter"
            FalseImage="ms-appx:///Assets/Inactive.ico"
            TrueImage="ms-appx:///Assets/Red.ico"
            />
    </UserControl.Resources>
    <tb:TaskbarIcon
        x:Name="TrayIcon"
        x:FieldModifier="public"
        ContextMenuMode="SecondWindow"
        LeftClickCommand="{x:Bind ShowHideWindowCommand}"
        NoLeftClickDelay="True"
        ToolTipText="WinHDR Utilities"
>
        <tb:TaskbarIcon.ContextFlyout>
            <MenuFlyout AreOpenCloseAnimationsEnabled="False">
                <MenuFlyoutItem
            Command="{x:Bind ShowHideWindowCommand}"
            Text="Show/Hide Window"
            />
                <MenuFlyoutSeparator />
                <MenuFlyoutItem
            Command="{x:Bind ExitApplicationCommand}"
            Text="Exit"
            />
            </MenuFlyout>
        </tb:TaskbarIcon.ContextFlyout>

        <tb:TaskbarIcon.IconSource>
            <tb:GeneratedIconSource Text="❤️" Foreground="AliceBlue"/>
        </tb:TaskbarIcon.IconSource>
    </tb:TaskbarIcon>

</UserControl>